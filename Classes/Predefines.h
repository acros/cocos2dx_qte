#ifndef __PREDEFINES_H__
#define __PREDEFINES_H__

#include <string>

#if (CC_TARGET_PLATFORM == CC_PLATFORM_WIN32)

#endif

using namespace std;

class GlobalSettings
{
public:
	static string	SOLDIER_ATTACH_NODE;

};

#endif